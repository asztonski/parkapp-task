schema {
  query: Query
  mutation: Mutation
}

type Query {
  ping: String!
}

type Mutation {
  login(email: String!, password: String!): AuthPayload!
}

type AuthPayload {
  token: String!
  user: User
}

type User {
  id: ID!
  email: String!
}
